groups:
  - name: docker_alerts
    rules:
      - alert: ContainerDown
        expr: time() - container_logs > 60
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "🚨 [{{ $labels.server_type }}] Docker container is down!"
          description : |
            The container {{ $labels.name }} has been down for more than 1 minute. 
            **Last Logs** : {{$labels.logs}}          